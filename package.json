{
  "name": "blog.givingjar.org",
  "version": "1.0.0",
  "homepage": "http://blog.givingjar.org",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/givingjar/blog.givingjar.org.git"
  },
  "devDependencies": {
    "git-scripts": "^0.2.1",
    "grunt": "~0.4.5",
    "grunt-jekyll": "^0.4.3",
    "surge": "latest"
  },
  "git": {
    "scripts": {
      "pre-commit": "jekyll build --future --config _config.yml,_config.qa.yml --destination _site.qa"
    }
  }
}
